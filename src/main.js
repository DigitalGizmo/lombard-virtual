import './app.css'
import App from './App.svelte'
// import Model from './Model.svelte'

const app = new App({
// const app = new Model({
  target: document.getElementById('app'),
})

export default app
